@model Repas.Models.TypeRepas

@{
    ViewData["Title"] = "Details de type repas";
}

<p class="text-center bg-white p-3 fw-bold h3 my-3 text-capitalize">@ViewData["Title"]</p>

<div class="div-content border border-light-subtle">

    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Type)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Type)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Destination)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Destination)
        </dd>
    </dl>
</div>
<div class="mt-3">
    <a class="btn btn-success" asp-action="Edit" asp-route-id="@Model?.Id">Modifier</a> 
    <a class="btn btn-primary" asp-action="Index">retour a la liste des types Repas</a>
</div>
